interface:
  display_name: "Fdic Mcp Server"
  short_description: "Repo-specific patterns and workflows for fdic-mcp-server"
  default_prompt: "Use the fdic-mcp-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true